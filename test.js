promise = myaync =>
    setTimeout(() => {
        resolve("result");
    }, 1000);

async function name() {
    asdf;
    asdfasdf;
    asdfasdf;

    await findOne(callback(err, user));

    async function asd() {
        await asd(null, callback(user)); //a@sdasd
    }
}
