async function name() {
    asdf;
    asdfasdf;
    asdfasdf;

    await findOne(callback(err, user));

    async function asd() {
        await Use({ name: "vahe", _id: "123123123" }, callback(err, profile));
    }
}
